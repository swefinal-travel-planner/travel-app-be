package constants

const (
	// InvitationCooldownDuration is the duration in milliseconds that users must wait
	// before they can send friend requests to each other again after a rejection
	InvitationCooldownDuration = 24 * 60 * 60 * 1000 // 24 hours in milliseconds
)
